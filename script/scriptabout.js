const numGuest = document.querySelector('#guest');

const validNumGuest = () => {
    const acceptedNumber = (acceptedNumber.length < 1);
    if (!acceptedNumber) {
        guestError.innerText = 'invalid number';
    }
}